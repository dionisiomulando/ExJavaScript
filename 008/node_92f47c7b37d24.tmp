/*
function entrar(){
    var senha = '';
    do{
        senha = prompt("Digite sua senha: ");
        
        if(senha=='2112js')
            console.log("Entrando no sistema...");
        else
            console.log("Senha invalida, tente novamente!");
            console.log(senha +=1)
    }while(senha !='2112js');
}
let nota = 15
while (nota > 0 && nota < 10){
    //
    if (nota > 0 && nota < 10){
        console.log(`Nota ${nota} válida`)
    } else {
        console.log(`Nota ${nota} inválida`)
    }
}

for (let nota = 0; nota < 10; nota++){
    let nota = Number(prompt('Digite uma nota'))
    if (nota > 0 && nota < 10){
        console.log(`Nota ${nota} válida`)
    } else {
        console.log(`Nota ${nota} inválida`)
        nota = Number(prompt('Digite Novamente'))
    }
    
}*/
let nota = 2 //Number(prompt('Digite a nota '))
while (nota < 10){
    //let nota = Number(prompt('Digite a nota '))
    if (nota < 10){
        console.log(`Nota ${nota} válida`)
    } else{
        console.log(`Nota ${nota} inválida`)
        //let nota = Number(prompt('Digite Novamente'))        
    }
    
}
